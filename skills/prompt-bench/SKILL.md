---
name: prompt-bench
description: Benchmark prompt/context token footprint across org task types. Trigger on "prompt bench", "token benchmark", "/prompt-bench".
---

Run `python3 skills/prompt-bench/prompt-bench.py` from repo root.
Add `--run` to call claude for each task (slow). Output written to `prompt-bench.md`.
